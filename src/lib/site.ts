/** Canonical site origin for SEO (metadataBase, sitemap, canonical, JSON-LD). */
export const SITE_URL = (process.env.NEXT_PUBLIC_SITE_URL || "https://cohortbuy.com").replace(/\/$/, "");
export const SITE_NAME = "CohortBuy";
